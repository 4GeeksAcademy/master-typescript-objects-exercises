{
  "name": "master-typescript-objects-exercises",
  "version": "1.0.0",
  "private": true,
  "description": "55 auto-graded TypeScript exercises on structured object data. Practice interfaces, optional fields, and object manipulation with strict typing.",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "audit:signatures": "node scripts/audit-signatures.mjs",
    "regenerate:app-ts": "node scripts/regenerate-app-ts.mjs",
    "transpile:all": "node scripts/transpile-all.mjs",
    "fix:signatures": "node scripts/regenerate-app-ts.mjs && node scripts/transpile-all.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
